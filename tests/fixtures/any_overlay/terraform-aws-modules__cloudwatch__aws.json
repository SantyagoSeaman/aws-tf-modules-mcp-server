{
  "module_id": "terraform-aws-modules/cloudwatch/aws",
  "built_from_version": "5.7.2",
  "vars": {
    "root::fixture_cloudwatch_marker": {
      "examples": [
        "fixture_cloudwatch_marker = {\n  x = 1\n}"
      ],
      "field_names": [
        "x"
      ],
      "provenance": "example+names"
    }
  },
  "all_inputs": {
    "log-group": [
      {"name": "create", "type": "bool", "required": false, "default": "true", "description": "Whether to create the log group"},
      {"name": "name", "type": "string", "required": false, "default": "null", "description": "Name of the log group"},
      {"name": "name_prefix", "type": "string", "required": false, "default": "null", "description": "Creates a unique name beginning with this prefix (conflicts with `name`)"},
      {"name": "retention_in_days", "type": "number", "required": false, "default": "null", "description": "0, 1, 3, 5, 7, 14, 30, 60, 90, 120, 150, 180, 365, 400, 545, 731, 1096, 1827, 2192, 2557, 2922, 3288, or 3653 (0 = never expire)"},
      {"name": "kms_key_id", "type": "string", "required": false, "default": "null", "description": "ARN of KMS key used to encrypt log data"},
      {"name": "log_group_class", "type": "string", "required": false, "default": "null", "description": "`STANDARD` or `INFREQUENT_ACCESS`"},
      {"name": "skip_destroy", "type": "bool", "required": false, "default": "null", "description": "If `true`, remove from state instead of deleting on destroy"},
      {"name": "tags", "type": "map(string)", "required": false, "default": "{}", "description": "Tags to assign to the resource"},
      {"name": "fixture_extra_field", "type": "string", "required": false, "default": "null", "description": "Fixture-only field absent from the curated table, proving supersede completeness"}
    ]
  }
}
